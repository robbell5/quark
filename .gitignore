# Dependencies
node_modules/

# Build output
dist/
build/

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store

# Quark ephemeral per-ticket working dirs (when dogfooding the harness here)
.work/

# Superpowers planning scaffold — ephemeral specs/plans, kept local only
docs/superpowers/

# Local env / secrets — defensive, never commit (none exist today)
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
id_rsa
id_rsa.*
credentials*

# Local agent / editor config (portable; not reliant on a global ignore)
.claude/settings.local.json
